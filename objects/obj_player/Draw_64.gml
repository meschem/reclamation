
/*
var extend = 4

var xStart = x - camera_get_view_x(view_camera[0])
var yStart = y - camera_get_view_y(view_camera[0]) + (sprite_height / 2) + 3

draw_healthbar(
	xStart - (sprite_width / 2) - extend,
	yStart,
	xStart + (sprite_width / 2) + extend,
	yStart + 3,
	(hp / maxHp) * 100,
	c_black,
	c_red,
	c_red,
	0,
	true,
	false
)
*/
